@using ModelDto;
@{
    ViewBag.Title = "Clientes";
}
@model ModelDto.Ctrl_ClienteDto
<h2>Clientes</h2>

<script src="~/Scripts/Admin/Cliente.js"></script>
<script type="text/javascript">
    var BuscarCliente = "@Url.Action("BuscarCliente", "Admin")";
    var DetalleCliente = "@Url.Action("DetalleCliente", "Admin")";
    var modelJson = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model));
</script>

<div class="content-heading">
    <div>
        Usa el siguiente formulario para mostrar los <b>clientes</b> registrados en la aplicaci&oacute;n.
    </div>

</div>

<div class="card card-default" style="border-color: #aaaaaa; border-width: 1px; border-style: solid; margin:1%">
    <div class="card-header">
    </div>
    <div class="card-body">
        <form id="formBuscadorCliente">
            <div class="form-group">
                <div class="col-md-4 col-sm-4 form-group">
                    <label for="Nombre" class="control-label font-weight-bold">Nombre cliente</label>
                    @Html.TextBoxFor(model => model.Nombre, new { @class = "form-control ", @id = "Nombre", @maxlength = 60 })
                </div>
                <div class="col-md-4 col-sm-4 form-group">
                    <label for="Pais" class="control-label font-weight-bold">Pais cliente</label>
                    @Html.TextBoxFor(model => model.Pais, new { @class = "form-control ", @id = "Pais", @maxlength = 60 })
                </div>

            </div>
            <div class="form-group" style="text-align:right">
                <div style="margin-right:20px">
                    <button type="submit" id="btnBuscar" class="btn btn-primary">Buscar</button>
                    <button type="button" id="btnRegistrar" class="btn btn-success">Registrar</button>
                </div>
            </div>
        </form>
    </div>
    <hr />

    <div class="card card-default containerGridBuscador">
        <div class="card-header">Resultados de b&uacute;squeda</div>
        <div class="card-body">
            <div class="container-fluid">
                <!-- DATATABLE-->
                <div class="card">
                    <div class="card-body">
                        <table id="gridBuscador" width="100%" class="table table-striped table-bordered table-hover compact">
                            <thead>
                                <tr>
                                    <th>Nombre</th>
                                    <th>Rfc</th>
                                    <th>Pais</th>
                                    <th>Email</th>
                                    <th>Mercado</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

